name: Build and Push to ECR

on:
  push:
    branches:
      - dev
    paths:
      - 'client/**'

jobs:
  call-workflow:
    uses: ./.github/workflows/ecr-push.yml
    with:
      work-dir: client
    secrets: inherit