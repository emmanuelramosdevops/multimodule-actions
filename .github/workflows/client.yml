name: Client - Build + Push ECR

on:
  push:
    branches:
      - dev
    paths:
      - 'client/**'

jobs:
  call-workflow:
    uses: ./.github/workflows/ecr-push.yml
    with:
      work-dir: client
      if: ${{ github.ref == 'refs/heads/dev' }}
      environment: Dev
    secrets: inherit